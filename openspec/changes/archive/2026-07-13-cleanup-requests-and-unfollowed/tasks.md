## 1. Payload: фильтр заявок

- [x] 1.1 В `app/dashboard.py` при сборке `lists.recent_follow_requests` исключить пользователей, уже входящих в `snapshot.followers` (по username)
- [x] 1.2 Счётчик `counts.recent_follow_requests` считать по отфильтрованному списку
- [x] 1.3 Убрать `recently_unfollowed` из `lists` (и из `counts`, если больше нигде в UI не нужен) либо оставить в данных, но не использовать в навигации — предпочтительно не отдавать во вкладки

## 2. UI V2

- [x] 2.1 В `dashboard_v2.py`: удалить tab `recently_unfollowed` из `TABS`
- [x] 2.2 Переименовать tab/карточку `lost_followers`: «Отписались» → «Отписались от меня» (label, desc при необходимости, карточка обзора)
- [x] 2.3 Проверить, что «Вы отписались» и «Заявки» без лишних упоминаний файла IG

## 3. UI V1

- [x] 3.1 В `dashboard_v1.py`: удалить tab `recently_unfollowed` из `TABS`
- [x] 3.2 Убрать карточку/метрику «Недавно отписаны» с обзора V1
- [x] 3.3 Выровнять подписи `lost_followers` на «Отписались от меня» (карточка обзора, если ещё «Отписались от вас»)

## 4. Документация и проверка

- [x] 4.1 Обновить таблицу вкладок в `README.md` (нет «Недавно отписаны»; «Отписались от меня»; заявки = не принятые)
- [x] 4.2 Перегенерировать дашборд (`generate`) и визуально проверить: заявки пустые/без принятых; нет вкладки «Недавно отписаны»; подписи изменений
- [x] 4.3 `openspec validate --all`
