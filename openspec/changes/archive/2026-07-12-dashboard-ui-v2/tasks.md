## 1. Freeze V1

- [x] 1.1 Вынести текущий HTML-шаблон и render-логику UI в `app/dashboard_v1.py` без изменения внешнего вида
- [x] 1.2 Оставить общую сборку payload (counts/lists/snapshots/profiles) в `app/dashboard.py` (или shared helper), чтобы V1/V2 не дублировали бизнес-логику
- [x] 1.3 Подключить генерацию `index-v1.html` из V1-шаблона
- [x] 1.4 Проверить, что `index-v1.html` открывается и ведёт себя как текущий дашборд

## 2. Генерация двух файлов

- [x] 2.1 Обновить generate-путь (`app/main.py` / storage), чтобы всегда писать `index.html` и `index-v1.html` из одного payload
- [x] 2.2 Убедиться, что background regenerate (если есть) тоже обновляет оба файла
- [x] 2.3 Добавить в шапку V1 кнопку-ссылку «Новый вид →» на `index.html`

## 3. Dashboard V2 — каркас

- [x] 3.1 Создать `app/dashboard_v2.py` со стилем Dark Refined (teal accent, без эмодзи-навигации, без purple glow)
- [x] 3.2 Сделать full-width main (без max-width 1200px), сайдбар + pages
- [x] 3.3 Добавить в шапку V2 кнопку «← Классический вид» → `index-v1.html`
- [x] 3.4 Подключить `index.html` = рендер V2

## 4. V2 — навигация и обзор

- [x] 4.1 Сайдбар: группы Главное / Анализ / Изменения / Списки; «Списки» свёрнуты по умолчанию; нейтральные числовые бейджи
- [x] 4.2 Обзор: period bar → крупные +/− → 4 кликабельные карточки изменений → метрики «Сейчас»
- [x] 4.3 Сохранить логику сравнения снимков (пресеты + выбор снимка) в более спокойном UI
- [x] 4.4 Empty-state, когда нет снимка для сравнения

## 5. V2 — списки

- [x] 5.1 Богатые строки: аватар, имя, @username, stats, «Открыть»
- [x] 5.2 Поиск по имени/@username
- [x] 5.3 Приглушение + текстовый бейдж для deactivated
- [x] 5.4 Списки изменений (requiresChanges) работают с выбранным периодом

## 6. Документация и проверка

- [x] 6.1 Обновить README: два файла, переключатель, V2 по умолчанию
- [x] 6.2 Прогнать `generate` на реальных/тестовых данных и вручную проверить V2 и откат на V1
- [x] 6.3 `openspec validate --all` — change и specs без ошибок
